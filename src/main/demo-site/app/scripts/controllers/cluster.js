'use strict';

/**
 * @ngdoc function
 * @name demoApp.controller:ClusterCtrl
 * @description
 * # ClusterCtrl
 * Controller of the demoApp
 */
angular.module('demoApp')
  .controller('ClusterCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
