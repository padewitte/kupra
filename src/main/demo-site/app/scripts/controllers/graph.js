'use strict';

/**
 * @ngdoc function
 * @name demoApp.controller:GraphCtrl
 * @description
 * # GraphCtrl
 * Controller of the demoApp
 */
angular.module('demoApp')
  .controller('GraphCtrl', function ($scope) {
    $scope.dbCtrl = {};
    $scope.colCtrl = {};
  });
